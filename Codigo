#include <cstdio>

int main () {
  int x= 7, y = 1;
  if (x < 0)  {
    printf ("El número es negativo");
  }
  else {
    for (int i = 1; i <= x; ++i) {
      y = y * i;
    }
    printf("el factorial de %d es %d", x, y);
  }
}
